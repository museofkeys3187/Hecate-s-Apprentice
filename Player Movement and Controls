using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerControls : MonoBehaviour
{
    public float spd;
    private Rigidbody2D playerRb;
    Vector2 playerMovement;
    public float hearts;
    public float enemyAttackDamage;
    public GameObject projectile;
    public float spellSpd;
    // Start is called before the first frame update
    void Start()
    {
        playerRb = GetComponent<Rigidbody2D>();
    }

    // Update is called once per frame
    void Update()
    {
        playerMovement.x = Input.GetAxisRaw("Horizontal");
        playerMovement.y = Input.GetAxisRaw("Vertical");
        playerRb.MovePosition(playerRb.position + playerMovement * spd * Time.fixedDeltaTime);
        if (Input.GetButtonDown("attack"))
        {
            GameObject spell = Instantiate(projectile, transform.position, transform.rotation);
            //spell.velocity = transform.forward * spellSpd;
        }
       
    }
    void OnTriggerenter2d(Collider2D other)
    {
        Debug.Log("contact");
        if (Input.GetButtonDown("attack"))
        {
            //run the attack animation
            //delete enemy on collision
            //run enemy death animation
            //delete enemy death animation
        }
        else
        {
            //hearts = hearts - enemyAttackDamage;
        }
    }
}
